import { HiloInputDirective } from './hilo-input.directive';

describe('HiloInputDirective', () => {
  it('should create an instance', () => {
    const directive = new HiloInputDirective();
    expect(directive).toBeTruthy();
  });
});
